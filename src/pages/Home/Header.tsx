import styled from "styled-components";
import Nav from "./Nav";
import Map from "./Map";

const Style = styled.div({})

type Props = {};

const Main = ({}) => {
  return (
    <Style>
      <Nav />
      <Map />
    </Style>
  );
};

export default Main;
